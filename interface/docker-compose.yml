version: "3.9"

services:
  neuroanalyse:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      # Frontend (read-only)
      - ./frontend:/app/frontend:ro
      # Data directory (database + reports) — persisted
      - ./data:/app/data
      # Project root mounts (model checkpoints, mask, demo scans)
      - ../DementiaMask_AAL3.nii:/app/DementiaMask_AAL3.nii:ro
      - ../model_runtime:/app/model_runtime:ro
      - ../mri_demos_nii:/app/mri_demos_nii:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
